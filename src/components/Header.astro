<header id="lawe-header" class="site-header">
    <nav class="site-nav">
        <div class="nav-container">
            <a href="/" class="logo-link a-no-style">
                <img src="https://flowbite.com/docs/images/logo.svg" alt="Flowbite Logo" class="logo-img" />
                <span class="logo-text">Lore Archive</span>
            </a>

            <div class="nav-actions">
                <button class="mobile-search-toggle">
                <!-- SVG icon -->
                </button>
                <div class="search-wrapper">
                    <div class="search-icon"></div>
                    <input type="text" id="search-navbar" class="search-input" placeholder="Search..." />
                </div>
                <button class="mobile-menu-toggle">
                <!-- SVG icon -->
                </button>
            </div>

            <div class="nav-links" id="navbar-search">
                <div class="mobile-search">
                    <div class="mobile-search-icon"></div>
                    <input type="text" class="search-input" placeholder="Search..." />
                </div>
                
                <ul class="menu-list">
                    <li><a href="#" class="menu-item current">Home</a></li>
                    <li><a href="#" class="menu-item">About</a></li>
                    <li><a href="#" class="menu-item">Services</a></li>
                </ul>
            </div>
        </div>
    </nav>
</header>


<style>
        /* Base header */
    .site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 50;
    transition: transform 300ms;
    height: 72px; /* as you had */
    }

    /* Nav container */
    .site-nav .nav-container {
    max-width: 1280px; /* approx for “screen-xl” */
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    padding: 1rem; /* p-4 */
    }

    /* Logo */
    .logo-link {
    display: flex;
    align-items: center;
    gap: 0.75rem; /* space-x-3 */
    }

    .logo-img {
    height: 2rem; /* h-8 */
    }

    .logo-text {
    font-size: 1.5rem; /* text-2xl, approx */
    font-weight: 600;  /* semibold */
    white-space: nowrap;
    }

    /* Nav actions (search, mobile toggles) */
    .nav-actions {
    display: flex;
    align-items: center;
    }

    /* Mobile search toggle */
    .mobile-search-toggle {
    background: none;
    border: none;
    color: #6B7280; /* gray-500 */
    padding: 0.625rem; /* approx p-2.5 -> ~0.625rem */
    border-radius: 0.375rem; /* rounded-lg approximated */
    cursor: pointer;
    }

    .mobile-search-toggle:hover {
    background-color: #F3F4F6; /* bg-gray-100 */
    }

    /* Search wrapper */
    .search-wrapper {
    position: relative;
    display: none; /* hidden on mobile, “hidden md:block” logic */
    }

    .search-wrapper .search-icon {
    position: absolute;
    left: 0.75rem; /* ps-3 / padding-start 3 */
    top: 50%;
    transform: translateY(-50%);
    pointer-events: none;
    }

    .search-wrapper .search-input {
    width: 100%;
    padding: 0.5rem 0.5rem 0.5rem 2.5rem; /* space for icon */
    font-size: 0.875rem; /* text-sm */
    color: #111827; /* text-gray-900 */
    background-color: #F9FAFB; /* bg-gray-50 */
    border: 1px solid #D1D5DB; /* border-gray-300 */
    border-radius: 0.375rem; /* rounded-lg */
    }

    .search-wrapper .search-input:focus {
    outline: none;
    border-color: #3B82F6; /* border-blue-500 style */
    box-shadow: 0 0 0 1px #3B82F6;
    }

    /* Mobile menu toggle */
    .mobile-menu-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem; /* w-10 == 2.5rem */
    height: 2.5rem; /* h-10 == 2.5rem */
    padding: 0;
    background: none;
    border: none;
    color: #6B7280; /* gray-500 */
    border-radius: 0.375rem;
    cursor: pointer;
    }

    .mobile-menu-toggle:hover {
    background-color: #F3F4F6;
    }

    /* Nav links */
    .nav-links {
    display: none; /* hidden by default / “hidden md:flex” logic */
    width: 100%;
    }

    .mobile-search {
    display: block; /* only on mobile in your Tailwind code */
    position: relative;
    margin-top: 0.75rem; /* mt-3 */
    }

    .mobile-search .mobile-search-icon {
    position: absolute;
    left: 0.75rem;
    top: 50%;
    transform: translateY(-50%);
    pointer-events: none;
    }

    .mobile-search .search-input {
    width: 100%;
    padding: 0.5rem 0.5rem 0.5rem 2.5rem;
    font-size: 0.875rem;
    background-color: #F9FAFB;
    border: 1px solid #D1D5DB;
    border-radius: 0.375rem;
    }

    /* Menu list */
    .menu-list {
    list-style: none;
    padding: 0;
    margin-top: 1rem; /* mt-4 */
    display: flex;
    flex-direction: column;
    gap: 0.5rem; /* maybe space between li items if you had something like space-y */
    font-weight: 500; /* font-medium */
    background-color: #F9FAFB; /* bg-gray-50 */
    border: 1px solid #E5E7EB; /* border-gray-100-ish */
    border-radius: 0.375rem;
    }

    @media (min-width: 768px) {
    /* md:flex behavior for menu list */
    .nav-links {
        display: flex;
        width: auto;
    }
    .menu-list {
        margin-top: 0;
        flex-direction: row;
        gap: 2rem; /* md:space-x-8 => 8 * 0.25 = 2rem */
        background: none;
        border: none;
        border-radius: 0;
    }
    }

    .menu-item {
    padding: 0.5rem 0.75rem; /* py-2 px-3 => 0.5rem vertical, 0.75rem horizontal */
    text-decoration: none;
    color: #111827; /* gray-900-ish */
    border-radius: 0.25rem; /* approx small rounded */
    }

    .menu-item:hover {
    background-color: #F3F4F6; /* hover:bg-gray-100 */
    }

    .menu-item.current {
    background-color: #1D4ED8; /* blue-700, example */
    color: white;
    }

    @media (min-width: 768px) {
    .menu-item {
        background: transparent;
        color: #1D4ED8; /* md:text-blue-700 */
        padding: 0;
    }
    .menu-item:hover {
        background: none;
        color: #1D4ED8;
    }
    .menu-item.current {
        background: transparent;
        color: #1D4ED8; /* md:dark:text-blue-500 etc you'd handle dark mode separately */
    }
    }

</style>