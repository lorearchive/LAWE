---
import Parser from '../scripts/Parsing/parser.ts'
import type { ASTNode } from '../scripts/Parsing/parser.ts'


export interface Props {
    slug: string[];
    content: string;
    title?: string;
}

const { slug, content, title } = Astro.props


// Convert raw wiki text to HTML using  parser
const htmlContent = Parser.parse(content)

---