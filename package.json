{
  "name": "lawe",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run build:wasm && astro dev",
    "prebuild": "npm run build:wasm",
    "build": "npm run build:wasm && astro build",
    "build:wasm": "cd rust-wasm && wasm-pack build --target web --out-dir ../rust-wasm/pkg",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "@astrojs/svelte": "^7.2.4",
    "@floating-ui/dom": "^1.7.6",
    "astro": "^5.16.6",
    "astro-sveltia-cms": "^0.1.0",
    "svelte": "^5.38.8",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^22.16.5"
  }
}
